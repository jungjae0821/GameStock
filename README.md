# GameStock

모바일 게임 데이터를 바탕으로 가상 종목을 거래하는 모의주식 서비스입니다.

## 프로젝트 핵심 목적

GameStock은 **게임 뉴스와 사용자·봇 거래 데이터를 기반으로 가격이 형성되는 과정을 체험하는 교육형 모의투자 플랫폼**입니다.

주식 거래 화면을 단순히 흉내 내는 데 그치지 않고, 뉴스·거래량·호가·시장 참여자가 가상 가격에 어떤 영향을 주는지 사용자가 직접 확인하고 거래하면서 학습할 수 있도록 하는 것을 목표로 합니다. 주요 대상은 게임 산업과 투자 개념에 관심이 있는 학생·초보 투자자이며, 실제 자산이 아닌 가상 화폐만 사용합니다.

현재 버전은 MySQL에 시장·계좌·주문·체결 데이터를 저장하는 교육용 모의주식 서비스입니다. 종목 조회, 시장 이벤트 조회, 매수·매도, 보유 자산 계산이 실제로 동작하며 서버를 다시 시작해도 거래 상태가 유지됩니다.

이 문서는 다음 순서로 읽으면 됩니다: 핵심 목적 → 실행 방법 → 프로젝트 구조 → 외부 데이터 연동 → 개발 진행 상황 → 졸업 프로젝트 보완 계획 → 기획 메모.

## 실행 방법

PowerShell에서 프로젝트 폴더를 연 뒤 다음을 실행합니다.

프로젝트 루트의 `scripts\start.cmd`를 더블클릭하면 백엔드와 웹 화면이 창 없이 실행됩니다. 실행한 창을 닫아도 서버는 계속 유지됩니다.

```powershell
Set-ExecutionPolicy -Scope Process Bypass
.\scripts\start.ps1
```

PowerShell에서 직접 실행할 때는 위 명령을 사용하세요. 서버를 종료하려면 `scripts\stop.cmd`를 더블클릭하면 됩니다.

브라우저에서 [http://localhost:8080](http://localhost:8080)을 엽니다.

Java 17 이상이 설치되어 있어야 합니다. 실행 스크립트는 Microsoft OpenJDK 17을 사용합니다.

백엔드 실행 전에 프로젝트 루트에 `.env`를 만들고 `DB_PASSWORD`에 MySQL 비밀번호를 한 번 입력합니다. `.env`는 Git에 포함되지 않으며, 이후에는 비밀번호를 다시 입력하지 않아도 됩니다.

### 컴퓨터 재부팅 후 MySQL 설정

MySQL이 설치되어 있어도 서버 서비스가 실행 중이 아니면 백엔드가 `localhost:3306`에 연결하지 못합니다. 처음 한 번 관리자 권한 PowerShell에서 MySQL을 Windows 서비스로 등록하고 자동 시작으로 설정합니다.

```powershell
& "C:\Program Files\MySQL\MySQL Server 8.0\bin\mysqld.exe" `
	--install MySQL80 `
	--defaults-file="C:\ProgramData\MySQL\MySQL Server 8.0\my.ini"

Set-Service MySQL80 -StartupType Automatic
Start-Service MySQL80
```

정상 설정 여부는 다음 명령으로 확인할 수 있습니다.

```powershell
Get-Service MySQL80
```

`Status`가 `Running`이고 `StartType`이 `Automatic`이면 컴퓨터를 재부팅해도 MySQL이 자동으로 실행됩니다. 서비스 등록 후에는 `start.ps1`이 백엔드 실행 전에 MySQL 서비스 상태를 확인하고, 중지되어 있으면 자동으로 시작합니다.

MySQL과 백엔드의 로컬 연결에는 인터넷이 필요하지 않습니다. 단, RSS에서 최신 뉴스를 수집하려면 인터넷 연결이 필요합니다.

## 현재 구조

```text
backend/                  Spring Boot REST API 및 실시간 WebSocket 서버
frontend/                 웹 화면 (HTML, CSS, JavaScript)
mobile/                   Expo 기반 Android/iOS 앱
server/src/.../           기존 단독 실행 데모 서버
database/schema.sql       MySQL 전환용 초기 테이블 구조
scripts/run-backend.ps1   Spring Boot 서버 실행 스크립트
scripts/run.ps1           웹 화면용 기존 데모 서버 실행 스크립트
scripts/start.ps1         백엔드와 웹 화면을 한 번에 실행하는 시작 스크립트
scripts/start.cmd         실행 정책 우회 후 start.ps1을 호출하는 바로가기
scripts/stop.ps1          실행 중인 백엔드와 웹 화면 종료 스크립트
scripts/stop.cmd          stop.ps1을 호출하는 종료 바로가기
```

## 개발 진행 상황

### 완료된 초기 작업

1. MySQL 서버 초기 설정 후 `database/schema.sql` 적용
2. JDBC 기반 MySQL 저장소 연결 및 종목·주문·포트폴리오 저장

### 기존 개발 기록 및 추가 아이디어

1. 로그인·회원가입 추가 (완료: Firebase Google 로그인, 사용자별 자산/주문, 출석 보상)
2. 봇 주문과 주문장/체결 엔진 고도화 (완료: 지정가 주문, 가격·시간 우선 매칭, 봇 호가, 호가창 API/화면)
3. 모바일 앱에 매수·매도 화면 추가 (완료: Google 로그인, 시장가/지정가 주문, 호가 깊이 그래프, 전체 체결 내역)
4. 각 종목별 화면에서 종목 이름 옆에 각 게임에 맞는 어플리케이션 사진 넣을거임
5. 앱 배포는 지금 이 방식에서 apk 방식으로 배포할거임
6. 모의주식이니까 출석체크 보상같은것도 있으면 좋을듯? (첫 로그인 하면 10만원, 2일째 로그인하면 20만원.....5일째 로그인하면 50만원을 주고 6일째 로그인부터는 출석보상 상향없이 50만원으로 고정 -> 만약 중간에 로그인하는걸 하루 빼먹으면 다시 첫 로그인부터 다시 카운트되고 로그인 기준 날짜는 자정)
7. (모바일 웹사이트 공통) 다크모드 기능 키고 킬수있도록 -> 기본값은 라이트모드로

## 외부 데이터 연동

### 게임 뉴스 수집

백엔드는 `backend/src/main/resources/application.yml`에 설정된 종목별 RSS 피드를 서버 시작 후와 10분 간격으로 조회합니다. 새 뉴스는 `market_events` 테이블에 중복 없이 저장되고, 기존 웹 화면의 시장 이벤트 영역에 표시됩니다.

뉴스는 게임명 일치만으로 저장하지 않습니다. 종목별 게임 별칭과 게임 문맥(업데이트·출시·캐릭터·플레이 등)을 함께 확인하고, 부동산·정치·스포츠·금융 등 명확한 비게임 문맥은 제외합니다. 게임 전문 출처는 짧은 제목도 허용하되 비게임 신호가 강하면 제외하며, API 응답에서도 같은 필터를 적용해 기존 오탐 노출을 줄입니다.

뉴스 영향도는 제목과 본문을 분리해 계산합니다. `서비스 종료·해킹·개인정보 유출` 같은 강한 부정 표현은 -3~-4점, `매출 증가·정식 출시·대규모 업데이트` 같은 강한 긍정 표현은 +3점, 일반 표현은 ±1~2점으로 계산합니다. 제목 신호는 본문보다 2배 반영하고, `출시 지연`처럼 부정 표현에 포함된 긍정 단어는 중복 계산하지 않습니다. 최종 점수는 -10~+10으로 제한되며, 양수·음수·0은 각각 상승 요인·하락 요인·보합 판단에 사용됩니다.

RSS 수집을 끄려면 실행 전에 다음 환경변수를 설정합니다.

```powershell
$env:GAMESTOCK_NEWS_ENABLED = "false"
```

피드 주소나 검색어를 바꾸려면 같은 `application.yml`의 `gamestock.news.feeds` 항목을 수정합니다. 형식은 `종목코드|RSS주소`입니다. 외부 RSS 서버가 응답하지 않아도 기존 시장 API와 거래 기능은 계속 작동합니다.

### 한강 수온

웹·모바일 계정 메뉴는 [한강 수온 사이트](https://xn--939at9l4tgt7l6ps.com/ko)의 표에서 `선유` 측정소 행만 읽어 수온을 표시합니다. 서버는 사이트를 30분마다 조회하고 마지막 성공값을 캐시합니다. 사이트가 잠시 실패하면 마지막 성공값을 유지하고, 아직 성공한 값이 없으면 조회 불가로 표시합니다.

기본 조회 주소는 `https://xn--939at9l4tgt7l6ps.com/ko`이며 `GAMESTOCK_HANGANG_SITE_URL` 환경변수로 바꿀 수 있습니다. 기능을 끄려면 `GAMESTOCK_HANGANG_ENABLED=false`로 설정하고, 갱신 주기는 `GAMESTOCK_HANGANG_REFRESH_MS`로 조정합니다. 이 사이트는 공개 API 대신 페이지 HTML을 읽으므로 사이트의 화면 구조가 바뀌면 파서 수정이 필요할 수 있습니다.

## 주식시장 유사성 개선 계획

현재 GameStock은 화면, 호가창, 지정가 주문, 부분 체결, 사용자별 자산 계산을 갖춘 교육용 모의주식 서비스입니다. 실제 거래소의 동작에 더 가깝게 만들려면 아래 순서로 보완합니다.

### 0순위: 기타옵션

- [x] 첫 Google 로그인 후 닉네임 설정 화면 표시
- [x] 로그인 전·후 모두 메뉴 토글을 표시하고, 비로그인 메뉴 최상단에 Google 로그인 버튼 제공
- [x] 마이페이지에 닉네임·보유 종목·수량·평균 매입가·실시간 평가 손익 표시
- [x] 메뉴에서 봇을 제외한 사용자 투자 랭킹 조회

### 1순위: 주문·체결의 정확성

- [x] `trades` 체결 테이블을 추가해 하나의 주문에서 발생한 개별 체결을 별도로 저장
- [x] 시장가 주문도 현재가 즉시 체결이 아니라 매도·매수 호가를 순서대로 소진하며 체결
- [x] 지정가 매수 주문의 현금과 지정가 매도 주문의 주식을 주문 시점에 예약
- [x] 예약된 금액·수량을 체결·취소·만료 시 정확히 반환
- [x] 부분 체결마다 체결 수량, 체결 가격, 체결 시각을 기록
- [x] 주문 취소 API와 화면 추가 -> 체결 예약인 주문과 관련된 모든 화면은 마이페이지 안에 넣으면 될거같긴함
- [x] 가격·시간 우선 매칭에서 실제 체결을 유발한 주문과 대기 주문을 구분하고 체결 가격 규칙을 명확히 적용

### 2순위: 가격 형성 및 시장 규칙

- [x] 봇이 항상 매수·매도 주문을 교차시키지 않도록 현실적인 스프레드와 유동성 모델 적용
- [x] 사용자 주문량, 체결량, 호가 잔량을 가격 변동에 반영
- [x] 종목별 호가 단위(tick size)와 주문 가능 가격 검증
- [x] 거래 시간, 장 시작·마감, 장외 시간 주문 처리 추가 -> 24시간 계속 거래할수 있도록
- [ ] 일일 가격 제한폭, 거래 중단, 급등락 보호 규칙 추가 -> 하지마 도파민 터지는 방법은 저런거 없이 급등락이 완벽해야됨
- [x] 시장가 주문에 호가 부족·슬리피지 상황을 반영

※ 일일 가격 제한폭·거래 중단 규칙은 급등락을 제한하지 않기를 원한 요청에 따라 이번 범위에서 제외했습니다.

### 3순위: 계좌·결제 현실화

- [x] 거래 수수료 계산 (매수·매도 체결 금액의 0.10%, 주문 응답·마이페이지에 표시)
- [x] 체결 즉시 현금·주식 반영 및 체결 완료 내역 표시 (T+1 지연 결제 제거)
- [x] 평균 매입가, 실현 손익, 평가 손익을 분리 계산 (수수료를 원가·실현손익에 반영)
- [ ] 배당, 액면분할, 종목 변경 등 기업 이벤트 지원 -> 종목은 지금 3개에서 바뀔일이 없으니 안해도 ㄱㅊ을지도?
- [ ] 공매도·대주를 지원할 경우 담보와 강제청산 규칙 추가 -> 안해요
- [ ] 잔고 변경과 체결 기록에 대한 감사 로그 추가 -> 이게뭐야 일단 빼봐

### 4순위: 시세·뉴스·운영 안정성

- [x] 고정 초기값 대신 검증 가능한 외부 시세 또는 재현 가능한 시뮬레이션 데이터 사용 -> `GAMESTOCK_SIMULATION_SEED`와 틱 상태를 저장해 같은 DB 스냅샷의 봇 흐름 재현
- [x] 뉴스의 방향과 강도를 가격 변동 모델에 반영 -> RSS 제목·본문의 긍정/부정 키워드를 -10~+10 영향도로 계산하고 최근 24시간 가격 모멘텀에 제한적으로 반영
- [x] 체결 기반 일별 시가·종가·거래량 집계 -> 시가는 해당 날짜 첫 기준가(자정 이후 초기화), 종가는 마지막 체결가로 저장하며 `/api/stocks/{code}/daily`에서 조회 (고가·저가는 이번 범위에서 제외)
- [x] 단일 서버의 `synchronized` 처리 대신 다중 인스턴스에서도 순서를 보장하는 주문 처리 직렬화 -> `market_locks` 행 잠금으로 주문·봇 매칭을 DB에서 한 번에 직렬화
- [x] 중복 주문 방지, 요청 속도 제한, 장애 복구와 재처리 정책 추가 -> 동일 주문 2초 중복 차단, 사용자 10초당 20건 제한, 트랜잭션 롤백으로 실패 주문 자동 복구
- [x] Firebase 인증 외에 운영자 권한, 로그인 보호, 개인정보 보관 정책 정비 -> `users.role`(USER/ADMIN), Firebase 토큰 검증, 보호된 `/api/admin/health`, 공개 랭킹의 프로필 이미지 비노출

### 5순위: 급하진 않은데 있으면 재밌을만한거

- [x] 인생 리셋 버튼 -> Google 계정별 1회만 실행 가능하며, 확인 후 현금·보유주식·미체결 주문·출석 기록·개인 거래 내역을 초기화하고 닉네임을 다시 설정하게 함. 시장 전체 체결 원장은 다른 투자자의 정합성을 위해 보존
- [x] 한강물 온도 표시 -> `https://xn--939at9l4tgt7l6ps.com/ko`의 선유 측정소 수온을 백엔드에서 30분마다 조회·캐시하고 웹/모바일 메뉴에 표시. 사이트 장애 시 마지막 조회값 또는 조회 불가 상태를 표시

## 졸업 프로젝트 보완 계획

현재 기능 구현은 완료되었지만, 졸업작품으로 제출할 때는 다음 내용을 추가해 프로젝트의 목적·근거·검증 가능성을 강화합니다.

### 1. 문제 정의와 사용자 가치

- 해결하려는 문제: 게임 이용자는 게임 뉴스가 기업·게임 가치에 미치는 영향을 안전하게 체험하기 어렵고, 초보 투자자는 주문·호가·체결 과정을 연습할 환경이 부족함
- 핵심 사용자: 게임 산업에 관심 있는 학생·초보 투자자
- 제공 가치: 실제 돈 없이 뉴스 해석, 주문 방식, 호가와 가격 형성의 관계를 반복 학습
- 성공 기준: 사용자가 뉴스 확인부터 주문·체결·손익 확인까지의 흐름을 이해하고 다시 사용할 이유가 있는지 사용자 테스트로 확인

### 2. 가격 형성 모델 문서화

- 가격 변동에 반영되는 요소와 비율을 명시: 뉴스 영향도, 최근 가격 모멘텀, 사용자 체결량, 봇 체결량, 호가 불균형
- 각 요소의 영향 범위와 상한을 기록해 하나의 뉴스가 가격을 독점적으로 움직이지 않도록 설명
- 같은 시드와 같은 DB 상태에서 동일한 시뮬레이션을 재현할 수 있도록 입력·결과 예시를 보관

### 3. 뉴스 필터·감성 분석 검증

- 게임명만 포함된 오탐을 제외하는 관련성 규칙과 긍정·부정 키워드 가중치를 문서화
- 직접 분류한 뉴스 표본을 만들어 관련성 정확도, 긍정·부정·중립 판정 정확도와 오탐 사례를 측정
- RSS 원문·출처의 이용 조건을 확인하고, 수집 실패·중복·오래된 뉴스 처리 방식을 기록

### 4. 거래 엔진과 서비스 품질 검증

- 시장가·지정가·부분 체결·주문 취소·예약 자산 반환에 대한 시나리오 테스트 작성
- 동시 주문에서도 잔고·체결·호가가 일치하는지 정합성 검증
- API 응답 시간, WebSocket 가격 갱신 지연, 서버 재시작 후 데이터 복구 여부를 측정

### 5. 배포·구조 정리

- 현재 사용 중인 Spring Boot 백엔드와 웹·Expo 앱을 기준 구조로 명시하고, `server/src` 기존 데모 서버의 역할과 유지 여부를 정리
- 실행·환경변수·Firebase·MySQL 설정을 처음 설치하는 사람이 재현할 수 있도록 문서화
- APK 또는 테스트 배포본을 준비하고, 개인정보·Firebase 서비스 계정·RSS 출처를 배포 환경에 맞게 점검

### 6. 기능 우선순위 관리

- 핵심 발표 범위: 뉴스 수집·필터링, 설명 가능한 가격 모델, 주문·체결, 실시간 화면, 손익·랭킹
- 시즌제·미션·공유 카드 등 반복 이용 기능은 핵심 흐름을 검증한 뒤 추가
- 한강 수온·인생 리셋·출석 보상은 부가 기능으로 분리해 핵심 목적과 혼동되지 않도록 표시

### 메모용

#### 현재 고민

1. 왜 이 서비스에 투자해야 됨? 게임사와 협업하지 않는 비공식 서비스만으로는 메리트가 부족할 수 있음. 협찬이 생기면 주간 수익 1·2·3위 사용자에게 소정의 보상을 제공하는 방안을 검토.
2. 실제 사용자가 얻는 혜택은 무엇인가? 주간 랭킹에서 좋은 성적을 낸 사용자에게 프로필 꾸미기 아이템 등을 제공하는 방안을 검토.
3. 구조 자체는 괜찮지만 전체 서비스로 보면 부족한 느낌이 있음. 졸업 프로젝트라는 큰 틀에서 서비스의 목적과 반복 이용 이유를 더 명확히 해야 함.

#### 우선적으로 추가하면 좋은 기능

1. 메인 화면에서 게임 뉴스와 가격 변동 이유를 함께 표시
   - 구현 완료: 메인 화면 종목 카드와 시장 이벤트에 최근 가격 흐름·뉴스 영향도·변동 이유를 표시하고, 뉴스 제목을 누르면 요약·영향 방향·게시 시각을 상세 모달로 확인할 수 있음. 상승 영향은 빨간색, 하락 영향은 파란색으로 표시.
2. 1~2주 단위 시즌제와 시즌별 랭킹 도입
   - 시즌 종료 후 초기화할지, 기존 주식·현금을 유지한 채 다음 주간 경쟁을 시작할지 결정 필요.
3. 뉴스 확인, 특정 수익률 달성, 손실 제한 등 투자 미션 추가
   - 프로필과 랭킹에 표시되는 칭호를 보상으로 제공하는 방안을 검토.
4. 뉴스·거래량·시장 이벤트·봇 거래가 가격에 준 영향 설명
   - 복잡한 AI보다 최근 뉴스 영향도, 거래량 변화, 호가 불균형을 근거로 한 규칙 기반 설명부터 구현.
5. 친구나 커뮤니티에 올릴 수 있는 투자 성적 공유 카드 제공
   - 포트폴리오와 시즌 성적을 카드 이미지로 저장하거나 공유하는 기능을 검토.
